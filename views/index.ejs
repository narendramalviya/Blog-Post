<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Document</title>
	</head>
	<body>
		<h1>Hii</h1>
		<!-- <%=JSON.stringify(result); %>  -->

		<div style="border: 2px solid black; width: 1000px;">
            <% if(result){ rows = result.forEach(post => { %>
                    <div style="border: 2px solid black; width: 400px;">
                        <h3><%=post.title%> </h3>
                        <p><%= post.body %> </p>
                    </div>
            <% });} %>
		</div>
	</body>
</html>
